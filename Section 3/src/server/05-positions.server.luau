-- 05 Vector3 Positions --

local wallPart: Part = game.Workspace.WallPart

-- wallPart.Position = Vector3.new(0, 10, 0)
-- wallPart.Rotation = Vector3.new(45, 0, 0)

-- wallPart.Position = wallPart.Position + Vector3.new(0, 5, 5)

-- wallPart.Size *= 1.5
